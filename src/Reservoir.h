#ifndef RESERVOIR_H
#define RESERVOIR_H

#include <string>

class Reservoir {
public:

private:
    std::string name;
    std::string municipality;
    int id;
    std::string code;
    int max_delivery;
};

#endif //RESERVOIR_H
